import 'package:flutter/material.dart';
import 'package:my_first_project/gradient_container.dart';

void main() {
  runApp(
    MaterialApp(
      home: Scaffold(body: GradientContainer.purple()), // Scaffold
    ),
  ); // MaterialApp
}
